---
name: frontend-react-playbook
description: Route React frontend requests to the proper atomic skill. Use when users ask for React UI implementation, rendering/performance optimization, or React best-practice enforcement and a high-level React entry skill is needed.
---

# Frontend React Playbook (Router)

## Routing rules

- React rendering/performance/state/update patterns → `react-best-practices`
- If task is primarily visual/UI craft beyond React internals, combine with `frontend-design` or `ui-ux-pro-max` as needed.

## Output requirement

Declare selected atomic skill(s) first, then execute.
